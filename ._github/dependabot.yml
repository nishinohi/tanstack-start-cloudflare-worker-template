version: 2
updates:
  # npm dependencies (pnpm uses npm ecosystem)
  - package-ecosystem: 'npm'
    directory: '/'
    target-branch: 'develop'
    schedule:
      interval: 'weekly'
      day: 'monday'
      time: '09:00'
      timezone: 'Asia/Tokyo'
    open-pull-requests-limit: 10
    groups:
      # Group TanStack packages together
      tanstack:
        patterns:
          - '@tanstack/*'
      # Group React packages together
      react:
        patterns:
          - 'react'
          - 'react-dom'
          - '@types/react'
          - '@types/react-dom'
      # Group dev tools together
      dev-tools:
        patterns:
          - 'eslint*'
          - 'prettier*'
          - 'stylelint*'
          - 'vitest'
          - '@testing-library/*'
      # Group Vite and plugins together
      vite:
        patterns:
          - 'vite'
          - '@vitejs/*'
          - 'vite-*'
      # Group Tailwind CSS packages together
      tailwind:
        patterns:
          - 'tailwindcss'
          - '@tailwindcss/*'
          - 'tailwind-*'
    ignore:
      # Node.js の型定義は開発時点の LTS バージョンに固定
      - dependency-name: '@types/node'
      # ESLint v10 は @typescript-eslint/utils との非互換性があるため v9 に固定
      - dependency-name: 'eslint'
        versions: ['>=10']
    labels:
      - 'dependencies'
      - 'automated'
    commit-message:
      prefix: 'chore'
      include: 'scope'

  # GitHub Actions
  - package-ecosystem: 'github-actions'
    directory: '/'
    target-branch: 'develop'
    schedule:
      interval: 'weekly'
      day: 'monday'
      time: '09:00'
      timezone: 'Asia/Tokyo'
    labels:
      - 'dependencies'
      - 'github-actions'
      - 'automated'
    commit-message:
      prefix: 'ci'
